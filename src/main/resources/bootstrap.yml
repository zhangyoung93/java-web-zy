spring:
  application:
    name: java-web-zy
  cloud:
    nacos:
      config:
        enabled: true
        namespace: namespace-zy
        file-extension: yml
        server-addr: localhost:8848
      discovery:
        enabled: true
        namespace: namespace-zy
        server-addr: localhost:8848